import { {{ModelName}}Entity } from 'src/app/modules/{{toKebab singularName}}/core/entities/{{toKebab ModelName}}.entity';

export class Prisma{{ModelName}}Mapper {
  static fromAppToPrisma(data: {{ModelName}}Entity) {
    return {
      ...data,
      ...(!data.id && {
        createdAt: data.createdAt ?? new Date(),
      }),
    };
  }
}
